<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.lizec.smartreview.mapper.ReviewStateDao">

    <insert id="insert">
        INSERT INTO knowledge_review_state(knowledge_id, review_count, memory_level, interval_time, next_review_time)
        VALUES (#{knowledgeId}, #{reviewCount}, #{memoryLevel}, #{intervalTime}, #{nextReviewTime})
    </insert>


    <update id="updateReviewState">
        UPDATE knowledge_review_state
        SET review_count     = #{reviewCount},
            memory_level     = #{memoryLevel},
            interval_time    = #{intervalTime},
            next_review_time = #{nextReviewTime}
        WHERE knowledge_id = #{knowledgeId}
    </update>


    <select id="selectById" resultType="top.lizec.smartreview.entity.KnowledgeReviewState">
        SELECT knowledge_id, review_count, memory_level, interval_time, next_review_time
        FROM knowledge_review_state
        WHERE knowledge_id = #{kid}
    </select>
</mapper>